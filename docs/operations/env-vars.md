# Examanet Environment Variables

> Auto-generated by `scripts/backup/env-vars-doc.mjs` on 2026-07-09T13:37:11.482Z

This document lists every environment variable the project uses, where it is defined, and whether it is sensitive.

**NEVER commit secret values to Git.** This file shows only keys, sources, and redacted placeholders.

## How to restore

1. Create `.env.local` at the project root
2. Copy each variable below from the last known good source (1Password, Vercel dashboard, or this backup)
3. Run `npx prisma generate` after editing
4. Verify with `node -e "require('dotenv').config(); console.log(process.env.DATABASE_URL?.length)"`

## Production secrets in Vercel

Production values live in **Vercel Dashboard → Project → Settings → Environment Variables**.
Vercel OIDC token does NOT grant API access to read them back — you must copy them manually.

## Variable inventory

| Variable | Type | Defined in | Length |
|----------|------|------------|--------|
| `BLOB_READ_WRITE_TOKEN` | 🔒 secret | .env, .env.example | 🔒 17 chars |
| `DATABASE_URL` | 🔒 secret | .env, .env.example, .env.local, .vercel/.env.development.local | 🔒 64 chars |
| `EMAIL_FROM` | 📋 public | .env | 34 chars |
| `I_LOVE_API_KEY` | 🔒 secret | .vercel/.env.development.local | 🔒 81 chars |
| `I_LOVE_API_PUBLIC_KEY` | 🔒 secret | .vercel/.env.development.local | 🔒 85 chars |
| `JOTFORM_API_KEY` | 🔒 secret | .env.local | 🔒 32 chars |
| `NEXTAUTH_SECRET` | 🔒 secret | .env, .env.example | 🔒 54 chars |
| `NEXTAUTH_URL` | 📋 public | .env, .env.example | 21 chars |
| `NEXT_PUBLIC_SITE_URL` | 📋 public | .env.local | 20 chars |
| `OPENAI_API_KEY` | 🔒 secret | .env | 🔒 164 chars |
| `RESEND_API_KEY` | 🔒 secret | .env, .env.example, .env.local | 🔒 36 chars |
| `SEED_TOKEN` | 🔒 secret | .env, .env.local | 🔒 32 chars |
| `UPLOAD_DIR` | 📋 public | .env, .env.example | 16 chars |
| `VERCEL_OIDC_TOKEN` | 🔒 secret | .vercel/.env.development.local | 🔒 1202 chars |

## Required for production

These must be set in Vercel dashboard for the prod environment:

- `BLOB_READ_WRITE_TOKEN` (✅ set)
- `DATABASE_URL` (✅ set)
- `I_LOVE_API_KEY` (✅ set)
- `I_LOVE_API_PUBLIC_KEY` (✅ set)
- `JOTFORM_API_KEY` (✅ set)
- `NEXTAUTH_SECRET` (✅ set)
- `OPENAI_API_KEY` (✅ set)
- `RESEND_API_KEY` (✅ set)
- `SEED_TOKEN` (✅ set)
- `VERCEL_OIDC_TOKEN` (✅ set)

## Verification

After restoring, run:

```bash
# Check all required vars are set
node scripts/backup/env-check.mjs
```
